<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.chatappjava">

    <!-- Permission -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Chatappjava.Navy"
        android:networkSecurityConfig="@xml/network_security_config"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules">

        <!-- Splash Activity (entry point) -->
        <activity
            android:name=".ui.auth.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth Activities -->
        <activity android:name=".ui.auth.LoginActivity" android:exported="true" />
        <activity android:name=".ui.auth.RegisterActivity" android:exported="true" />

        <!-- Chat Activities -->
        <activity android:name=".ui.chat.ChatListActivity" android:exported="true" />
        <activity android:name=".ui.chat.PrivateChatActivity" android:exported="true" />

        <!-- Group Chat Activities -->
        <activity android:name=".ui.chat.GroupChatActivity" android:exported="true" />
        <activity android:name=".ui.chat.CreateGroupActivity" android:exported="true" />\

        <!-- Settings Activity -->
        <activity android:name=".ui.settings.SettingsActivity" android:exported="true" />
        
        <!-- Friend Activities -->
        <activity android:name=".ui.friends.FriendListActivity" android:exported="true" />
        <activity android:name=".ui.friends.FriendRequestActivity" android:exported="true" />
        <activity android:name=".ui.friends.AddFriendActivity" android:exported="true"  />

        <!-- Callactivity -->
        <activity android:name=".ui.call.VideoCallActivity" android:exported="true"/>
        <activity android:name=".ui.call.IncomingCallActivity" android:exported="true"/>
    </application>

</manifest>
